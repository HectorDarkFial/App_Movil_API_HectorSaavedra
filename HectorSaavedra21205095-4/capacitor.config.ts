import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'HectorSaavedra21205095-4',
  webDir: 'www'
};

export default config;
